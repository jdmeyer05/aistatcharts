AI STATCHARTS — UX IMPROVEMENTS CHANGELOG
==========================================


================================================================================
March 24-25, 2026 — POWER ANALYTICS OVERHAUL
================================================================================
Scope: Complete rewrite of pages/23_Power_Analytics.py + new src/ercot_api.py
Files changed: pages/23_Power_Analytics.py, src/ercot_api.py, .streamlit/secrets.toml

ERCOT PUBLIC API INTEGRATION (src/ercot_api.py — new file):
  - Azure B2C OAuth token management with auto-refresh (1hr tokens)
  - Actual system load by weather zone (NP6-345-CD) — replaces generation proxy
  - Wind generation actual + forecast + capacity (NP4-732-CD)
  - Solar generation actual + forecast + capacity (NP4-737-CD)
  - Real-time settlement point prices by hub/zone (NP6-905-CD)
  - Day-ahead settlement point prices (NP4-190-CD)
  - SCED System Lambda — actual 5-min marginal clearing price (NP6-322-CD)
  - DAM System Lambda — day-ahead marginal price (NP4-523-CD)
  - Historical load (30-day) for trend analysis

DUCK CURVE TAB — 7 new analytical features:
  1. Historical overlay: 7-day and 30-day avg net load from ERCOT API actual demand
  2. Renewable penetration %: wind+solar as % of total generation over the day
  3. Flexibility metrics: ramp range, 3-hr directional ramp, ramp-to-peak ratio, severity grade
  4. Over-generation risk: margin above derived min gen constraint, hours at risk
  5. Storage arbitrage + price overlay: RT price on secondary y-axis, arb value with 85% RT efficiency
  6. Day-ahead forecast vs actual: load forecast error (MAE, MAPE, bias)
  7. Multi-ISO comparison: ERCOT/CAISO/MISO/PJM/SPP/NYISO via EIA Hourly Grid Monitor
  8. Wind & solar forecast vs actual: ERCOT API hourly gen + STPPF/STWPF forecast + capacity (HSL)
  9. DAM vs RT price spread: hourly overlay with spread bars
  10. Zonal hub prices: Houston/North/South/West congestion indicator

HEAT RATE TAB — 6 new analytical features:
  1. Hourly implied heat rate curve: RT and DAM by hour with CCGT/peaker reference bands
  2. System Lambda: ERCOT's actual 5-min marginal price overlaid with DAM Lambda
  3. Heat rate vs net load scatter: dispatch curve slope with trend line
  4. Heat rate duration curve: % of intervals at each efficiency level
  5. Gas price trend: 30-day chart with yfinance futures + EIA daily overlay
  6. Gas price priority: yfinance NG=F (real-time) > EIA daily > EIA weekly

SPARK SPREAD TAB — 7 new analytical features:
  1. VOM-adjusted margins: fuel cost + variable O&M for true profitability (EIA AEO/NREL ATB)
  2. Hourly spark spread curve: CCGT and CT net margin by hour
  3. Profitability hours: count + avg margin for each plant type
  4. DAM vs RT spark spread: expected vs realized CCGT profitability
  5. System Lambda vs fuel cost: ERCOT clearing price vs plant SRMC lines
  6. Spark spread duration curve: profit distribution sorted best-to-worst
  7. Proper fuel prices: nuclear=uranium ($0.65/MMBtu), coal=PRB/lignite ($1.50), gas=live futures

STACK ANALYSIS TAB — 6 new analytical features:
  1. Gas fleet disaggregation: CCGT (65%) / Steam (11%) / CT (24%) per ERCOT CDR 2024
  2. Inframarginal rent: $/hr gross margin by fuel type
  3. Fuel mix area chart: stacked 24-hour generation by fuel with demand overlay
  4. Belly vs peak comparison: side-by-side donut charts with swing table
  5. Reserve margin: actual MW with ERCOT EEA thresholds (3,000 MW EEA1, 1,750 MW EEA2)
  6. Supply curve fix: renewables use current output (not installed), storage excluded when charging

DATA ACCURACY FIXES:
  - Net load filtered to single day (was cross-day inflated)
  - Storage charging included in total_gen for correct demand approximation
  - Duck severity thresholds calibrated to ERCOT seasonal norms (not arbitrary)
  - All arbitrary values replaced with sourced/derived values
  - Gas price staleness warnings when EIA data >5 days old
  - Consistent gas price across all tabs (yfinance > EIA daily > weekly)
  - DAM SPP fetched for today (was returning tomorrow's)
  - Supply curve uses current generation for renewables (not installed capacity)

YFINANCE INTEGRATION:
  - NG=F front-month natural gas futures (real-time, 15-min delay)
  - CL=F WTI crude oil futures
  - Top metrics row: NG futures, WTI crude, ERCOT Hub Avg
  - Eliminates EIA gas price staleness (was 8+ days behind)


================================================================================
March 19, 2026 — UX IMPROVEMENTS
================================================================================
Date: March 19, 2026
Scope: 21 features across 25+ files, all 20 pages impacted


================================================================================
1. MOBILE SESSION RECOVERY — AUTO-RELOAD ON STALE CONNECTION
================================================================================
Files changed: src/layout.py

What changed:
  JavaScript is injected on every page that monitors when the phone goes to
  background. If the app was backgrounded for more than 60 seconds, the page
  automatically reloads when the user returns.

How it benefits users:
  Previously, mobile users returning to the app after locking their phone or
  switching apps would find all navigation buttons greyed out and unclickable.
  The only fix was to manually close and reopen the browser. Now the app
  silently recovers on its own — users pick up right where they left off.


================================================================================
2. PULL-TO-REFRESH BANNER
================================================================================
Files changed: src/layout.py

What changed:
  For shorter background periods (15–60 seconds) or after 5 minutes of idle
  time with no interaction, a yellow banner appears at the top of the screen:
  "Data may be stale — tap to refresh." Tapping it reloads the page.

How it benefits users:
  Users no longer have to guess whether the data on screen is current. The
  banner gives them a clear, low-friction way to refresh without hunting for
  a reload button or pulling down the browser address bar.


================================================================================
3. COOKIE-BASED AUTH PERSISTENCE
================================================================================
Files changed: src/auth.py, app.py

What changed:
  After login, the Supabase refresh token is stored in a browser cookie. When
  the session state is lost (server restart, long idle, mobile wake-up), the
  app reads the cookie and silently re-authenticates without showing the login
  screen.

How it benefits users:
  Mobile users previously had to re-enter their email and password every time
  the connection went stale. Now sessions survive app backgrounding, browser
  restarts, and even server redeployments. Users stay logged in seamlessly.


================================================================================
4. "REMEMBER ME" CHECKBOX
================================================================================
Files changed: app.py, src/auth.py

What changed:
  A "Remember me for 30 days" checkbox was added to the login form (checked
  by default). When checked, the auth cookie persists for 30 days. When
  unchecked, the cookie expires when the browser is closed.

How it benefits users:
  Users on shared or public devices can uncheck the box to ensure their
  session does not persist. Users on personal devices get the convenience
  of staying logged in for a month without re-authenticating.


================================================================================
5. LOGOUT BUTTON
================================================================================
Files changed: src/layout.py

What changed:
  A "Log Out" button was added to the sidebar on every page, along with a
  caption showing the signed-in email address. Logging out clears the auth
  cookie, wipes all session state, and redirects to the login screen.

How it benefits users:
  There was no way to sign out from within the app — users had to clear
  cookies manually. Now logout is one tap away from any page, and it
  properly cleans up all stored credentials.


================================================================================
6. SESSION TIMEOUT WARNING
================================================================================
Files changed: src/auth.py, app.py

What changed:
  The app tracks when the user authenticated. At 50 minutes, a toast
  notification warns: "Session expires soon — save your work." At 60 minutes,
  the app silently attempts to re-authenticate using the Supabase session or
  the stored cookie. If re-auth fails, the user is notified.

How it benefits users:
  Users running long AI analyses or reviewing results no longer get
  unexpectedly kicked to the login screen. They get advance warning and
  the app tries to renew the session automatically in the background.


================================================================================
7. RESPONSIVE NAVIGATION — MOBILE TOUCH TARGETS
================================================================================
Files changed: src/styles.py

What changed:
  Navigation buttons on phone screens now have a 44px minimum height (Apple's
  recommended tap target size), larger font, and generous padding. On screens
  under 640px, nav buttons wrap into rows of 3. On very small screens (under
  400px), they wrap into rows of 2 with 48px height.

How it benefits users:
  The 5-column nav bar was nearly unusable on phones — buttons were too small
  to tap accurately. Now every button is comfortably tappable even on small
  screens, and the layout adapts to the available width.


================================================================================
8. CACHED TICKER & ESCALATION DATA
================================================================================
Files changed: src/layout.py, src/ticker_tape.py

What changed:
  The escalation data reader (iran_conflict_history.json) was wrapped in
  @st.cache_data(ttl=300). The ticker tape data function now includes a
  _fetched_at timestamp for staleness detection.

How it benefits users:
  Every widget interaction previously re-read the JSON file from disk and
  re-fetched market data. Now these are cached for 5 minutes, making page
  interactions noticeably faster — especially on pages with many widgets.


================================================================================
9. SKELETON LOADERS & BATCH PREFETCH
================================================================================
Files changed: src/styles.py, src/layout.py, pages/01_Summary.py

What changed:
  A CSS shimmer animation and skeleton card component were added. The Summary
  page now prefetches all 12 market tickers upfront behind a loading skeleton,
  then renders all cards at once instead of one by one.

How it benefits users:
  On first load or cache expiry, the Summary page previously showed a blank
  screen for 10-20 seconds as cards trickled in one at a time. Now users see
  an animated loading indicator immediately, and all cards appear together
  once data is ready.


================================================================================
10. SMART AI TOKEN MANAGEMENT — NO BURN ON CACHE HITS
================================================================================
Files changed: pages/03_Stock_Analysis.py

What changed:
  AI model calls are cached for 30 minutes via @st.cache_data. Previously,
  clicking "Run Analysis" for the same ticker within that window still
  deducted a token even though the cached result was served. Now the app
  tracks a hash of the prompt — tokens are only deducted on genuinely
  fresh API calls.

How it benefits users:
  Users who re-run the same analysis (e.g., after navigating away and back)
  no longer waste their daily AI allowance or purchased tokens on cached
  results. This effectively stretches their quota further.


================================================================================
11. STALE DATA INDICATOR
================================================================================
Files changed: src/layout.py, src/ticker_tape.py

What changed:
  A red "DELAYED 23m" badge appears in the header when market data is more
  than 15 minutes old. The ticker strip timestamp is color-coded: green for
  fresh (<5 min), yellow for warming (5–15 min), red with a warning icon
  for stale (>15 min).

How it benefits users:
  Users making trading decisions need to know if the data on screen is
  current. The visual indicators make staleness immediately obvious,
  preventing decisions based on outdated prices.


================================================================================
12. AUTO-REFRESH DURING MARKET HOURS
================================================================================
Files changed: src/layout.py

What changed:
  The ticker strip is wrapped in @st.fragment(run_every=300), causing it to
  automatically re-render every 5 minutes. This triggers a fresh data fetch
  (the cache TTL also aligns at 5 minutes). The fragment updates without
  reloading the entire page.

How it benefits users:
  During market hours, users no longer need to manually refresh to see
  updated prices. The ticker strip updates itself in the background every
  5 minutes while the rest of the page remains interactive and unchanged.


================================================================================
13. RECENTLY VISITED PAGES
================================================================================
Files changed: src/layout.py

What changed:
  A "Recent:" row appears below the navigation bar showing the last 5 pages
  the user visited (excluding the current page). Each entry is a clickable
  link with the page icon.

How it benefits users:
  With 20 pages, users typically rotate between 4-5 favorites. The recent
  pages row saves them from opening dropdown menus to find pages they were
  just on. One tap to jump back.


================================================================================
14. DEEP LINKING — TICKER FOLLOWS YOU ACROSS PAGES
================================================================================
Files changed: src/layout.py, 11 page files

What changed:
  A shared active ticker system was added. When a user types "AAPL" on any
  page, it is stored in session state. When they navigate to another page,
  the ticker input pre-fills with "AAPL" instead of the default "SPY."
  URL deep linking is also supported via ?ticker=TSLA query parameter.

Pages updated: Stock Analysis, Historical Analysis, Options Analysis,
  Options Flow, Options Lab, ML Predictor, Tech Screener, Algo Backtester,
  Monte Carlo, RL Trading, Scenario Analysis (partial).

How it benefits users:
  Previously, switching from Stock Analysis to Options Flow for the same
  ticker meant retyping it. Now the ticker carries over automatically.
  Users can also share links like ?ticker=NVDA that open directly to
  their desired asset.


================================================================================
15. BREADCRUMB CONTEXT
================================================================================
Files changed: src/layout.py

What changed:
  The header bar now shows a breadcrumb: "AI STATCHARTS > Stock Analysis > AAPL"
  displaying the current page name and the active ticker being analyzed.

How it benefits users:
  After switching between several pages and tickers, users can glance at
  the header to instantly confirm what page they are on and which asset
  they are viewing. This is especially helpful on mobile where less
  context is visible.


================================================================================
16. ERROR BOUNDARIES ON ALL SECTIONS
================================================================================
Files changed: src/layout.py, src/styles.py, 14 page files

What changed:
  - Options Analysis: all 9 tabs individually wrapped (Vol Surface, IV Skew,
    Term Structure, Open Interest, Gamma Exposure, Max Pain, Unusual Activity,
    Greeks Heatmap, Chain View).
  - Iran Conflict: all 3 tabs wrapped.
  - 13 additional pages now import error_boundary for section-level use.
  - Streamlit's default exception display was restyled to match the app's
    dark theme with a red border and scrollable traceback.
  - A page_error_boundary() helper was added for wrapping entire pages.

How it benefits users:
  Previously, a single API failure or data error would crash an entire page,
  showing an ugly Python traceback. Now each section fails independently —
  if the IV Skew tab errors out, the other 8 tabs still render normally.
  Error messages are styled clearly with instructions to refresh.


================================================================================
17. LOADING STATES ON AI BUTTONS
================================================================================
Files changed: pages/03_Stock_Analysis.py, pages/19_Iran_Conflict.py,
               pages/02_Scenario_Analysis.py

What changed:
  AI analysis buttons now show "Running..." and become disabled while the
  analysis executes. They re-enable when results are ready.

How it benefits users:
  Users previously could double-click or triple-click the "Run Analysis"
  button while waiting, potentially burning multiple tokens for duplicate
  API calls. Now the button clearly communicates that work is in progress
  and prevents accidental re-triggers.


================================================================================
18. USAGE/ACCOUNT DASHBOARD
================================================================================
Files changed: pages/01_Summary.py

What changed:
  The Account section on the Summary page now includes:
  - A visual progress bar showing daily AI usage (green/yellow/red).
  - Session duration display (e.g., "Session active for 23m").
  - Improved logout that properly clears cookies and all session state.

How it benefits users:
  Users can see at a glance how much of their daily AI allowance they have
  used and how long their session has been active. The visual gauge makes
  it easy to pace usage throughout the day. The improved logout ensures a
  clean sign-out with no stale data left behind.


================================================================================
19. DAILY BRIEFING
================================================================================
Files changed: pages/01_Summary.py

What changed:
  A new "Daily Briefing" section appears near the top of the Summary page,
  generating a 3-5 sentence market summary. It synthesizes:
  - Live market data: equity index direction, VIX fear gauge, commodities
  - Cached AI analysis: Grok macro regime + market sentiment
  - Geopolitical risk: Iran conflict escalation (when elevated)
  The briefing is styled in an accent-bordered card and costs zero API
  calls — it is built entirely from already-cached data.

How it benefits users:
  Users get an instant, human-readable snapshot of market conditions the
  moment they open the app. No need to scan multiple charts or navigate
  to different pages — the briefing distills the key information into a
  few sentences. Especially valuable for mobile users who want a quick
  check-in.


================================================================================
20. WATCHLIST WITH PRICE ALERTS
================================================================================
Files changed: pages/01_Summary.py

What changed:
  A new "Watchlist" section on the Summary page allows users to:
  - Add tickers with optional price thresholds (alert above $X, below $X).
  - View mini price cards for each watchlist ticker (price + daily change).
  - Remove tickers with a single click.
  - Receive automatic alerts when:
    * Price crosses above or below their set thresholds.
    * Any watchlist ticker moves more than 5% in a day.
  Watchlist alerts appear in the existing Alerts section alongside regime
  shift and VIX alerts. The watchlist persists across page navigations
  within the same session.

How it benefits users:
  Users can track specific assets they care about without navigating to
  individual analysis pages. The price threshold alerts surface important
  moves automatically — users do not need to manually check prices. This
  turns the Summary page into a personalized command center.


================================================================================
21. STYLED EXCEPTION DISPLAY
================================================================================
Files changed: src/styles.py

What changed:
  Streamlit's default exception/traceback display (.stException) was restyled
  with a dark background, red border, rounded corners, and a scrollable,
  smaller-font traceback section.

How it benefits users:
  When errors do occur, they no longer look like a jarring white-background
  code dump. They match the app's visual design and are easier to read,
  with the traceback contained in a scrollable area that does not dominate
  the screen.


================================================================================
SUMMARY OF IMPACT
================================================================================

Mobile Experience:
  - Sessions no longer break when the phone goes to sleep
  - Navigation buttons are properly sized for touch
  - Stale data is clearly indicated with a tap-to-refresh option
  - Login persists across app restarts via cookies

Performance:
  - Summary page loads all market data behind a skeleton loader
  - Cached data avoids redundant API calls and file reads
  - AI tokens are not wasted on cached results
  - Ticker strip auto-refreshes without full page reloads

Navigation:
  - Tickers carry across all pages automatically
  - Recent pages are one tap away
  - Breadcrumbs show current context at all times
  - URL deep linking supports sharing specific views

Reliability:
  - Error boundaries prevent one bad section from crashing a page
  - Session timeout warnings prevent unexpected logouts
  - AI buttons prevent double-click token waste

Engagement:
  - Daily briefing gives instant market context on login
  - Watchlist with alerts turns the Summary into a command center
  - Usage gauge helps users pace their AI allowance
  - Account dashboard shows session and subscription status

Files Modified:
  src/auth.py, src/layout.py, src/styles.py, src/ticker_tape.py, app.py,
  and all 20 page files in the pages/ directory.


================================================================================
22. PERFORMANCE OPTIMIZATIONS — March 23, 2026
================================================================================

22a. REMOVED TICKER TAPE
    Files changed: src/layout.py, src/ticker_tape.py (now unused)
    What changed:
      Removed the scrolling ticker strip from the header entirely. This
      included the _render_ticker_strip() fragment, _get_ticker_tape_data()
      calls in render_header(), the ticker tape import, the warm-cache
      pre-fetch of ticker data, and the watchlist/scroll feed toggle in
      Settings.
    Impact:
      Eliminates Polygon batch API calls, social feed fetching (Grok +
      StockTwits), and ~50KB of HTML generation that ran on every page
      load and every 5-minute fragment refresh.

22b. REMOVED DUPLICATE SIDEBAR CSS
    Files changed: src/layout.py
    What changed:
      Removed the 18-line inline CSS block in setup_page() that hid the
      sidebar and killed top gaps. These rules were already present in
      styles.py inject_global_css(). The duplicate caused the browser to
      parse identical selectors twice on every page load.
    Impact:
      -500 bytes CSS and ~50ms parse overhead per page load.

22c. GATED CSS INJECTION WITH SESSION FLAG
    Files changed: src/layout.py
    What changed:
      inject_global_css() is now only called once per session, gated by
      st.session_state["_css_injected"]. Previously, 580+ lines (~23KB)
      of inline CSS were re-injected and re-parsed on every Streamlit
      rerun (any widget interaction).
    Impact:
      -200-500ms render time on every rerun after the first page load.

22d. PARALLELIZED SUMMARY PAGE DATA FETCHING
    Files changed: pages/01_Summary.py
    What changed:
      The sequential loop fetching 12 market tickers (each making 2-3
      Polygon API calls) was replaced with concurrent.futures
      ThreadPoolExecutor(max_workers=6). All 12 tickers now fetch in
      parallel.
    Impact:
      Summary page load: ~10s -> ~2s (5x faster).

22e. PARALLELIZED OIL FUNDAMENTALS EIA FETCHING
    Files changed: pages/14_Oil_Fundamentals.py
    What changed:
      10 sequential EIA API calls (inventories, production, Cushing,
      refinery, imports, exports, WTI, gasoline, distillate, product
      supplied) now run in parallel via ThreadPoolExecutor(max_workers=6).
    Impact:
      Oil page load: ~10s -> ~2s.

22f. PARALLELIZED NATGAS FUNDAMENTALS EIA FETCHING
    Files changed: pages/15_NatGas_Fundamentals.py
    What changed:
      8 sequential EIA API calls (national storage, 5 regional storage,
      Henry Hub price, monthly consumption) now run in parallel via
      ThreadPoolExecutor(max_workers=6).
    Impact:
      NatGas page load: ~8s -> ~2s.

22g. JAVASCRIPT OBSERVER IDEMPOTENCY
    Files changed: src/styles.py
    What changed:
      The Plotly scrollZoom MutationObserver in inject_global_css() now
      checks window._plotlyScrollGuard before creating a new observer.
      The mobile session guard and footer already had idempotency checks.
    Impact:
      Prevents MutationObservers from stacking on Streamlit reruns,
      reducing memory leaks and DOM mutation overhead.

22h. INCREASED INTRADAY CACHE TTL
    Files changed: src/data_engine.py
    What changed:
      polygon_intraday() cache TTL increased from 300s to 3600s. Intraday
      data only updates during market hours (9:30am-4pm ET), so the
      5-minute TTL caused pointless re-fetches after close.
    Impact:
      Eliminates unnecessary Polygon API calls during after-hours and
      weekends.

22i. REMOVED UNUSED SCIPY IMPORT
    Files changed: src/data_engine.py
    What changed:
      Removed top-level `from scipy.stats import norm` — the symbol was
      not referenced anywhere in the file.
    Impact:
      Saves ~5MB import overhead on every data_engine module load.

22j. LAZY SKLEARN IMPORT
    Files changed: src/simulation.py
    What changed:
      Moved `from sklearn.ensemble import RandomForestRegressor` from
      module level into the function that uses it. sklearn is ~200MB and
      was being loaded on import even when the simulation page was never
      visited.
    Impact:
      ~200MB deferred until the Monte Carlo simulation is actually run.

22k. LAZY OPENAI IMPORTS
    Files changed: pages/02_Scenario_Analysis.py, pages/03_Stock_Analysis.py,
                   pages/19_Iran_Conflict.py
    What changed:
      Moved `from openai import OpenAI` from top-level imports into the
      functions that create OpenAI clients. The library was being loaded
      on page import even when the user never triggered an AI analysis.
    Impact:
      ~500KB deferred per page until an AI model is actually invoked.

22l. NEW PAGE: POWER ANALYTICS
    Files changed: pages/23_Power_Analytics.py (new), src/layout.py, src/auth.py
    What changed:
      Added a new Power Analytics page with 4 tabs:
      - Duck Curve: net load profile (demand minus wind/solar), ramp rates
      - Heat Rate: implied market heat rate from power price / gas price
      - Spark Spread: profitability by plant type (CCGT, CT, steam, coal)
      - Stack Analysis: merit order dispatch with supply curve
      Page registered in PAGE_CONFIG, added to Energy nav group and free tier.
    Data sources:
      ERCOT dashboard API (fuel mix, supply/demand, settlement point prices),
      EIA API (Henry Hub gas price).

22m. SUMMARY PAGE REDESIGN — MARKET HEATMAP
    Files changed: pages/01_Summary.py
    What changed:
      Replaced the old market dashboard (12 mini-chart cards + relative
      performance line chart + HTML legend) with a new Market Heatmap
      section. The heatmap drills into ETF/index holdings and shows
      individual stocks grouped by sector or category.

      5 selectable lists:
      - Sectors: 11 SPDR sectors, ~8-10 top holdings each (~90 stocks)
      - Equity Indices: SPY, QQQ, DIA, IWM, etc. top 10 holdings each
      - Fixed Income: 10 bond ETFs
      - Commodities: 8 commodity ETFs
      - Mega Caps: 16 top names grouped by sub-category

      Each group shows a header with name + avg return, then a card grid
      where each stock has ticker, period return (green/red intensity-
      scaled background), and current price. Period selector (1M/3M/YTD/
      1Y) controls the return window. All stock data fetched in parallel
      with ThreadPoolExecutor(10) and cached 5 minutes.

      Removed: mini-chart sparklines, relative performance line chart,
      HTML legend, performance cards, _perf_card(), _render_card_grid(),
      _fetch_relative_data(), _mini_chart(). Prefetch reduced from 12
      tickers to 1 (VIX for alerts only).
    Impact:
      Summary page API calls reduced from 36+ to ~1 for initial load.
      Heatmap fetches ~80-100 stocks in parallel on first render, cached
      thereafter. Much more informative at a glance.

22n. HEADER CLEANUP — REMOVED STATUS BADGES
    Files changed: src/layout.py
    What changed:
      Removed the AFTER-HRS/LIVE/PRE-MKT badge, tier badge (Platinum),
      usage badge (50/50), and timestamp from the header bar. Now shows
      only the logo + AI STATCHARTS brand name. Market status, tier, and
      usage info moved into the Settings popover.
      Removed dead _build_usage_badge() function and unused auth imports
      (get_user_tier, get_tier_config, get_usage_summary).
    Impact:
      Cleaner header, less visual clutter. All account/status info is
      still accessible via Settings.

22o. SETTINGS POPOVER — FULLY BUILT OUT
    Files changed: src/layout.py
    What changed:
      Settings popover now has 4 sections:
      - Account: email, plan name with tier color
      - AI Usage: daily analyses progress bar (color-coded), token balance,
        upgrade prompt for free users
      - Market: live status dot (green/yellow/gray) + current time ET
      - App: version, data sources, AI models
      - Actions: Log Out button (now visible for all users including local dev)
    Impact:
      All platform status info in one place. Previously only had email +
      logout.

22p. CSS & SPACING IMPROVEMENTS
    Files changed: src/styles.py, src/layout.py
    What changed:
      - Added 10px bottom padding to header bar for breathing room between
        brand and nav dropdowns
      - Set margin-top/padding-top to 0 on h1/h2/h3 to tighten gap between
        nav row and page titles
      - Bumped chart line colors to higher saturation across all 5 lists
        (e.g. #00d1ff -> #00e5ff, #ff4444 -> #ff3333)
    Impact:
      Less wasted vertical space. Page content starts closer to nav bar.

22q. DEAD CODE REMOVAL — ~450 LINES
    Files changed: src/layout.py, src/data_engine.py, pages/01_Summary.py
    Files deleted: src/ticker_tape.py, _Home_backup.py
    What changed:
      With the ticker strip removed, large sections of layout.py became
      unreachable. Removed the following dead code:
      - _warm_caches() — only pre-fetched social/polymarket for the removed strip
      - _get_social_feed() — Grok + StockTwits fetcher (~100 lines)
      - _get_grok_ticker_posts() — Grok X/Twitter API caller (~80 lines)
      - _get_polymarket_scroll() — Polymarket odds fetcher
      - _ai_filter_social_posts() — Gemini-based post scorer
      - _is_clean_post(), _strip_urls(), _clean_tweet_tickers() — post filters
      - _PROFANITY_ROOTS, _PROFANITY_EXACT, _PROFANITY_RE — profanity patterns
      - _DEFAULT_SOCIAL_SYMBOLS, _MIN_FOLLOWERS, _APPROVED_HANDLES — constants
      - _SCROLL_POLYMARKET_SLUGS — Polymarket config
      - _get_escalation_data() — conflict JSON reader (only used by strip)
      - _is_market_hours() — never called anywhere
      - render_sidebar_brand() — no-op stub
      - render_skeleton_cards() — defined but never called
      Also removed dead imports:
      - `timezone` from layout.py
      - `numpy`, `make_subplots`, `render_skeleton_cards` from 01_Summary.py
      - `Client` (Supabase type) from data_engine.py
      Deleted src/ticker_tape.py (all functions dead) and _Home_backup.py
      (orphaned backup file).
    Impact:
      ~450 fewer lines of code loaded on every page. Eliminates Grok API,
      StockTwits, Polymarket, and Gemini calls that were running in
      background threads for no consumer. Reduces import overhead and
      memory footprint.


================================================================================
March 26-27, 2026 — SIGNAL SCANNER, PORTFOLIO OPTIMIZER, META ANALYSIS
================================================================================
Scope: Major upgrades to Signal Scanner (8 tabs), Portfolio Optimizer (9 methods),
new Meta Analysis page (3029 lines, 9 tabs), quant_features upgrades.
Files changed: pages/39_Signal_Scanner.py, pages/38_Portfolio_Optimizer.py,
pages/41_Meta_Analysis.py (new), src/quant_features.py, src/data_engine.py,
src/layout.py

SIGNAL SCANNER — EXPANDED FROM 4 TO 8 TABS:
  Files changed: pages/39_Signal_Scanner.py (complete rewrite, 1511 lines)
  1. Dashboard — market regime indicator, factor spread metrics, top/bottom picks,
     multi-factor signal heatmap
  2. Momentum — 12-1 momentum, acceleration, risk-adjusted, consistency, L/S spread
  3. Mean Reversion — RSI/BB/Z-score with confluence scoring (-3 to +3)
  4. Value & Quality (NEW) — valuation heatmap (PE/PB/EV-EBITDA/FCF/div yield),
     quality heatmap (ROE/margins/growth), value-vs-quality quadrant scatter,
     balance sheet risk table
  5. Earnings & Sentiment (NEW) — EPS revisions (30D net), insider buying/selling
     (90D net value), high-conviction combined signals
  6. Regime & Microstructure (NEW) — VPIN (informed trading probability), entropy
     (market orderliness), vol regime (20D/63D ratio), regime classification table
  7. Factor Correlation (NEW) — Spearman correlation matrix, redundancy alerts
     (|r|>0.7), conflict alerts (r<-0.3), eigenvalue decomposition, effective
     factor count
  8. Composite Ranking — configurable factor weights via sliders, weighted composite
     bar chart, top-10 factor profile heatmap, full ranking table
  New signals (12 added): momentum acceleration, risk-adjusted momentum, momentum
  consistency, MR confluence, ROE, profit/operating/gross margins, revenue/earnings
  growth, EPS revision ratio, insider buy/sell ratio, VPIN, entropy.
  Performance: parallel data loading (ThreadPoolExecutor), combined yfinance
  download for price+volume, parallelized fundamentals fetch.

PORTFOLIO OPTIMIZER — UPGRADED FROM 6 TO 9 METHODS:
  Files changed: pages/38_Portfolio_Optimizer.py, src/quant_features.py
  New allocation methods:
  - HERC (CVaR) — Hierarchical Equal Risk Contribution (Raffinot 2018), uses
    CVaR as risk metric instead of variance for tail-risk awareness
  - HCAA (1/N) — Hierarchical Clustering-Based Asset Allocation, equal-weight
    within cluster branches, ultra-robust lowest-turnover
  - Robust Max Sharpe added to walk-forward backtest (was missing)
  Upgrades:
  - HRP upgraded from single linkage to Ward linkage (more balanced clusters)
  - HRP/HERC/HCAA accept pre-computed denoised covariance/correlation
  - Ledoit-Wolf covariance denoising toggle (default ON) — sklearn LedoitWolf
    shrinkage applied before all clustering methods
  - Dendrogram visualization in Risk Analysis tab (Plotly figure_factory)
  - Estimation window sensitivity handles all 9 methods
  New shared functions in src/quant_features.py:
  - denoise_covariance() — Ledoit-Wolf shrinkage, returns annualized cov/corr
  - _hrp_linkage() — shared clustering helper for HRP/HERC/HCAA
  - herc_allocate() — HERC with CVaR or variance risk metric
  - hcaa_allocate() — hierarchical equal-weight allocation

META ANALYSIS PAGE (NEW — 3029 lines, 9 tabs):
  Files changed: pages/41_Meta_Analysis.py (new), src/layout.py
  Purpose: Institutional-grade cross-method portfolio comparison engine.
  Walk-forward backtests 9 allocation methods + custom blends + SPY benchmark
  on a configurable multi-group ticker universe.

  Controls:
  - 15 preset groups (Multi-Asset, 11 sectors, Mega Caps, Global Macro)
  - Individual ticker toggles with group auto-sync
  - Configurable lookback (1-5Y), rebalance (monthly/quarterly), est. window
  - Ledoit-Wolf denoising toggle
  - Forward estimates toggle (analyst targets, EPS revisions, valuation, macro)
  - Blended portfolio presets (Robust+MaxDiv 50/50, HRP+HERC 50/50, custom)

  Tab 1 — Equity Curves:
  - Walk-forward P/L paths for all methods + SPY Buy & Hold (white line)
  - Top-N slider, final value metrics

  Tab 2 — Allocations:
  - Current optimal weights (donut + position list)
  - All methods side-by-side weight table
  - Rebalance weight history heatmap
  - Portfolio turnover bar chart with annual cost estimate
  - CSV download button

  Tab 3 — Forecasts:
  - Forward return estimates blending 4 components:
    40% analyst implied return (yfinance consensus target)
    30% EPS revision momentum (net upgrades/downgrades)
    20% valuation signal (forward PE vs universe median)
    10% macro overlay (FRED yield curve + VIX regime)
  - Macro context dashboard (yield curve, VIX, fed funds, 10Y)
  - Component breakdown stacked bar chart
  - Historical vs forecast comparison
  - Analyst coverage detail table

  Tab 4 — Performance:
  - Ranked table (Return, Vol, Sharpe, Sortino, Max DD, Calmar, Win Rate)
  - Grouped bar chart comparing risk-adjusted metrics

  Tab 5 — Institutional Analytics:
  - Net-of-cost equity curves (gross dotted vs net solid, 10bps round-trip)
  - Cost drag table per method
  - Regime analysis (Bull/Bear/Crisis/Recovery Sharpe heatmap + return bars)
  - Up/Down capture ratio scatter + Information Ratio bar chart
  - Stress scenario analysis (2008 GFC, COVID, 2022 rates, etc. via beta)
  - Capacity estimate (1% ADV rule, bottleneck ticker identification)

  Tab 6 — Statistical Tests (de Prado):
  - Deflated Sharpe Ratio (adjusts for multiple testing bias)
  - Probability of Backtest Overfitting (CPCV with 6 blocks)
  - Sequential Bootstrap CI (20-day block bootstrap, 90% CI)
  - Minimum Track Record Length (days needed at 95% confidence)
  - Overfitting Scorecard (4-point PASS/FAIL per method, verdict)

  Tab 7 — Drawdown:
  - Underwater equity curves
  - Max drawdown comparison bar chart
  - Drawdown duration analysis (longest, average, episodes)

  Tab 8 — Rolling Analysis:
  - Rolling 63D Sharpe (top 5 methods)
  - Method correlation matrix (Spearman)
  - Independent vs redundant method pairs
  - Cumulative excess return vs Equal Weight

  Tab 9 — Universe Grid:
  - Backtests ALL 15 preset groups independently
  - Sharpe / Return / Max DD heatmaps (universes x methods)
  - Top 15 universe+method combinations
  - Best method per universe, best universe per method
  - Method consistency chart (avg Sharpe with std dev error bars)
  - Incremental analysis (Sharpe impact of adding each sector to Multi-Asset)
  - Hierarchical two-layer allocation:
    Layer 1: group-level optimization (how to split between groups)
    Layer 2: within-group best method (how to split within each group)
    Final combined portfolio with SPY comparison banner, CSV downloads,
    dollar allocation table

  Executive Summary (above tabs):
  - SPY comparison banner (table: Sharpe/Return/MaxDD with edge)
  - Top 8 positions as metric cards
  - CSV download button for current best portfolio weights

OPTIONS FIX — EXPIRATION DATE FETCH:
  Files changed: src/data_engine.py
  - get_expiration_dates() now filters to expired=false, contract_type=call,
    single-page fetch with 10s timeout
  - Added yfinance fallback (yf.Ticker.options) — instant, always works
  - SPY expirations now load in ~12s instead of timing out
