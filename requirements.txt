streamlit>=1.33.0
pandas>=2.1.0
numpy>=1.26.0
plotly>=5.18.0
supabase>=2.0.0
openai>=1.12.0
anthropic>=0.30.0
google-genai>=1.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
requests>=2.31.0
cachetools>=5.3.0
curl_cffi>=0.7.0
extra-streamlit-components>=0.1.60
stripe>=7.0.0
openpyxl>=3.1.0
flask>=3.0.0
statsmodels
shap
TA-Lib>=0.4.28
yfinance>=0.2.36
pdfplumber>=0.10.0
toml>=0.10.0

# API server — pinned to exact versions so each Cloud Build produces a
# reproducible image. Behaviour drift between patch releases of
# fastapi/pydantic/starlette has surfaced latent bugs in the past
# (e.g. jsonable_encoder's numpy-scalar strictness tightened between
# 0.115.x minors). Unpin only when deliberately testing a new major.
fastapi==0.135.3
uvicorn==0.42.0
pydantic==2.12.5
PyJWT>=2.8.0
cryptography>=42.0.0
slowapi==0.1.9

# Robinhood integration (admin-only endpoints: /api/positions/robinhood,
# /api/market/holding-deep-dive, /api/market/trade-architect)
robin_stocks>=3.0.0
